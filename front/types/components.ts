export enum IconType {
    ERROR = 'error',
    SUCCESS = 'celebration',
    INFO = 'info',
    WARNING = 'warning',
}

export enum AlertType {
    SUCCESS = 'success',
    ERROR = 'error',
    INFO = 'info',
    WARNING = 'warning',
}

export type Option = {
    label: string;
    value: string;
}